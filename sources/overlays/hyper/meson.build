project('hyper', 'c',
    version : '0.10.0',
    meson_version : '>=1.1.0',
)

install_data('BOOTX64.EFI', 'hyper_install', 'hyper_iso_boot',
    install_dir : get_option('prefix') / 'share',
    install_tag : 'bin'
)
