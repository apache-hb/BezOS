[binaries]
c = 'clang'
strip = 'strip'

[properties]
c_args = [
    '-m64',
    '-ffreestanding',
    '-fno-builtin',
    '-mno-sse',
    '-mno-sse2',
    '-mno-mmx',
    '-nostdlib',
    '-nostdinc',
    '-fno-stack-protector',
    '-mno-red-zone',
    '-mcmodel=kernel',
    '-fno-threadsafe-statics',
    '-fno-common' ]

c_link_args = [
    '-ffreestanding',
    '-nostartfiles',
    '-nostdlib',
    '-lgcc',
    '-fuse-ld=ld' ]

[host_machine]
system = 'bezos'
cpu_family = 'x86_64'
endian = 'little'
cpu = 'i686'
